<ul>
	<%= link_to "Add Photo", new_photo_path, :class => 'addpic' %>
	<%= link_to "Edit", new_photo_path %>
		<% current_user.photos.each do |u| %>
	<li>
		<%=image_tag u.image.url, width: "300" %><br />
			<%#= image_tag u.image.url %>
			<%#= image_tag u.image.url %>
			<%#= image_tag u.image.url %>
			<%= link_to "Erase", photo_path(u), :class => 'eraze' , method: :delete %>
		<% end %>
	</li> 
</ul> 




<!-- <h1>Loginasdfasfd</h1>

<h5> Need an account? </h5>
<%#if current_user %>
Welcome <%= current_user.name %>
<%#= link_to "Logout", session_path, method: :delete %>
<%# else %>
<%#= link_to "Sign Up", new_user_path if !@is_signup%>
<%#= link_to "Log In", new_session_path if !@is_login%>
<%# end %>

fasdfasdfadsfads

<%#= form_for @user, {url: session_path} do |f| %>
	<%#= f.text_field :email, placeholder: "E-mail" %>
	<%#= f.password_field :password, placeholder: "Password" %>
	<%#= f.submit "Log In" %>
<%# end %> -->